# 🌍 MASTER PRD (GLOBAL DESIGN + LAYOUT RULES)

You are continuing an existing student productivity website.

The file **homepage.html is already built and is the design reference**.

Your job is to ensure all new pages:

* Look EXACTLY like homepage.html in layout and styling
* Reuse existing CSS from style.css
* Do NOT introduce new design systems

---

## 🎯 CORE RULE

All pages must visually match homepage.html.

This includes:

* spacing
* alignment
* colors
* fonts
* layout structure

Do NOT redesign anything.

---

## 📌 STRICT CLASS USAGE RULE

ONLY copy and reuse these existing classes from homepage.html:

* Sidebar classes
* Page Header classes

Do NOT:

* Rename them
* Modify their structure
* Create alternative versions

---

## 🧩 REQUIRED LAYOUT STRUCTURE

Every page must follow this structure:

```html
<body>
  <!-- Sidebar (copied exactly from homepage) -->
  <div class="sidebar">
    ...
  </div>

  <!-- Main Content Area -->
  <div class="main-content">

    <!-- Page Header (copied exactly from homepage) -->
    <div class="page-header">
      ...
    </div>

    <!-- Page-specific content goes here -->
    <div class="content">
      <!-- UNIQUE CONTENT -->
    </div>

  </div>
</body>
```

---

## 🎨 DESIGN RULES

* Use ONLY styles already defined in style.css
* Do NOT create new global styles
* Do NOT change font sizes, colors, or spacing
* Maintain same padding/margins as homepage

---

## ⚙️ DEVELOPMENT RULES

* Use only HTML, CSS, JavaScript
* Do NOT use frameworks or libraries
* Do NOT duplicate CSS unnecessarily
* Keep structure clean and readable

---

## 🚫 RESTRICTIONS

* Do NOT redesign layout
* Do NOT create new sidebar styles
* Do NOT create new header styles
* Do NOT change existing class behavior

---

## ✅ EXPECTATION

When a new page is opened, it should feel like:

* It is part of the SAME application
* It was built together with homepage.html

There should be ZERO visual inconsistency.

---

## ⚠️ FINAL INSTRUCTION

Treat homepage.html as the **single source of truth** for layout.

Copy structure and classes EXACTLY where required.

Only the content inside the main content section should change per page.

---
